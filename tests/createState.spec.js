test("waitForA ", () => {
  //   function waitForA(c) {
  // 	startIndex = n;
  // 	++n;
  // 	if (c === "a") {
  // 	  // 继续看看下一个字符是不是b
  // 	  return waitForB;
  // 	}
  // 	return waitForA;
  //       }
  // 没有匹配到的 下一个状态
  // 逻辑 需要在逻辑里面来指定，所以就自定义把
  // createState(narmalState, function logic(c) {
  // 	if (c === "a") {
  // 	  // 继续看看下一个字符是不是b
  // 	  return waitForB;
  // 	}
  // });
});
